export interface MetricSchema {
  event_time: string;
  event_type: string;
  product_id: string;
  category_id: string;
  category_code: string;
  brand: string;
  price: string;
  user_id: string;
  user_session: string;
}
